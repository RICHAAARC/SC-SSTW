# Window-State-MSE-V1 paired candidate delivery

Baseline: `24e33ef8cfb5185b27953d311d57652c1065e474` on `dev/flow-fixed-key-v1`, with the old fixed-marker objective as the default. This isolated branch adds the explicit writer objective `window-state-mse-v1`: the mean squared error between existing \(\pm1\) states and the 11-by-2 aggregates \(t_{w,a}=\operatorname{mean}_{80}(\tanh(p_i)\,sync_i\,polarity_i)\). It retains temperature 1, the 1,760 supports, 44/46, and \(R^*=0.042943312697648145\). The receiver protocol, receiver search, calibration, and scoring are unchanged.

Candidate worktree: `/home/richar/projects/Video-WM/worktrees/Window-State-MSE-V1` on `dev/window-state-mse-v1`. The baseline worktree `/home/richar/projects/Video-WM/worktrees/Flow-Fixed-Key-V1` remains the source baseline at `24e33ef8cfb5185b27953d311d57652c1065e474`.

`runtime/wan/window_state_mse.py` owns the target. `fixed_key_control.clean_direction` retains the default legacy call shape and accepts an explicit objective. `generate_key_terminals`, `generate_video`, and `fixed_key_cli generate --objective` persist the writer-objective identity; receive accepts no objective. The canonical base protocol continues to describe the legacy default; `--objective` is an explicit writer override, and only `writer_objective` plus the writer records identify the actual target. A shared receiver protocol ID is not evidence that the writer objective was identical.

CPU checks used `/home/richar/projects/CEG-WM/alive/CEG-WM/.venv/bin/python` with Torch 2.5.1+cpu and pytest 9.1.1: `pytest tests/test_fixed_key.py -q -k "not notebook"` passed 16 tests, with one notebook test intentionally deselected. The concise actual output is [WINDOW_STATE_MSE_V1_CPU_TEST.log](WINDOW_STATE_MSE_V1_CPU_TEST.log). The checks cover the independent aggregate formula, all-1,760 analytic gradients and finite difference, nonuniform window/axis reweighting, equal signed-gap normalized-direction equivalence at nonzero aggregate \(t\), detached-double clean-leaf support/finite/nonzero/local descent, legacy default compatibility, explicit CLI routing, and fake-model/scheduler 44/46 accounting. The fake paired old/new calls use the same source configuration, initial noise, state44, and fresh scheduler snapshots; they capture each current \(z-\sigma v\) clean leaf, confirm the 46-step re-evaluation, per-step and cumulative \(R^*\), and no source-snapshot mutation.

## Paired runner and notebook source

The frozen experiment specification is [docs/window_state_mse_v1_paired_spec.md](docs/window_state_mse_v1_paired_spec.md). The new config and runner preserve the four source roster and generate 12 physical source-arm instances: calibration OFF only, then one shared OFF plus legacy/MSE SINGLE46 and MULTI44_46 for each evaluation source. One state-44 latent and complete scheduler snapshot feed five cloned forks per evaluation source. The complete expected calls are 4 generation, 496 transformer, 248 scheduler, 12 each zero-shadow/unit-probe/backward, 12 decode, 36 MP4 save/read, and 144 phase encodes.

Each saved arm-view is read once and encoded into four phase observations once. The same observations feed the unchanged ORIGINAL receiver and the pure `C1_MATCHED_CONFIRM`/`C2_STATE_CONFIRM` receiver in `main/tube_state/fixed_key_split_receiver.py`. Three thresholds are frozen independently from only the two calibration OFF sources. The result schema preallocates all 108 view-decision and 36 source-arm-decision slots, retains child/phase/receiver/count failures, records same-source legacy/MSE blind score and margin differences plus fixed-reference differences, and labels the shared OFF as one physical sample.

The notebook builder `scripts/build_window_state_mse_paired_notebook.py` requires an explicit immutable 40-hex source commit and accepts an output path. The source commit therefore contains the builder but no formally bound notebook. After source publication, the builder produces the sole Run-all notebook bound to that source SHA; the first cell is only the two required Drive-mount lines, setup and runner argv remain visible, a fresh Drive directory is used, and a nonzero runner exit still exposes a retained `result.json` while recording the return code.

The paired implementation passed `24` tests for `tests/test_fixed_key.py tests/test_window_state_mse_paired.py`. The seven paired checks are CPU/fake/static only: exact five-fork call accounting and snapshot isolation, one read/one four-phase encode per unique saved view with the same observations sent to all receivers, receiver-exception isolation, failed-phase calibration exclusion, calibration-before-evaluation, fixed result slots and paired reporting, truncated child-result retention, and standard-library notebook structure/AST plus exact reused install argv and nonzero-return handoff. A later receipt-only repair added the directly imported `generation`, `integrated_core`, `io`, and `vae` module files to the source receipt; its affected test passed `1 passed, 6 deselected in 0.73s`. The installed environment has no `nbformat`; no `nbformat` library validation is claimed. The main reviewer separately compared the portable split receiver with two saved real observation views and found every recursively compared field equal with maximum numeric difference 0.0; no model, VAE, download, or new media run was performed.

No GPU, model, Colab, or real-media experiment has been run for this candidate. Source publication, immutable notebook generation/binding, and remote readback occur only after the frozen source review. The formal notebook remains for the user to run.

## Paired implementation review

- A1 implementation — sole writer: completed the fixed paired core helper, runner, pure split receiver, parameterized notebook builder, failure-retaining report, and CPU/fake/static checks. The source candidate contains no formally bound notebook.
- A2 method review — accepted without substantive repair. The fixed roster, shared OFF, same-noise/state/scheduler pairing, writer objectives, 44/46 budget, three independent calibrations, blind/reference separation, fixed denominators, and claim ceiling match the adopted specification.
- A3 implementation and evidence review — accepted after one mechanical receipt repair. The runner now records the real module files for every directly imported generation/media adapter; its affected test passed. A3 found no remaining execution-path, receiver-isolation, calibration-order, failure-retention, count, or notebook-source issue.
- A4 synthesis — accepted the same frozen version. A2 and A3 agree that the candidate is ready for a local source commit, while GPU/model/media evidence remains entirely unrun.
- A5 milestone audit — accepted without substantive repair after the receipt fix. It confirmed the frozen runner/test/spec, the exact 13-file source-S scope with no `.ipynb`, the builder's required 40-hex immutable binding and source-S-before-notebook-N order, the 4/12/36/144 experiment denominator and 108/36 decision slots, independent receiver calibration, one shared physical OFF, and retained failures. It also confirmed the old Flow baseline remains clean and `main` remains `58776e57905b2b50de232b76a89101ae7229713a`. It did not rerun tests; all evidence remains CPU/fake/static with no real model, GPU, media, Colab, or Drive execution.

## Initial objective implementation review

- A1 implementation — `gpt-5.6-terra`, high: sole writer; completed the isolated route, the repaired coverage, and `16 passed, 1 deselected` in 9.78 seconds.
- A2 method review — `gpt-5.6-sol`, high: accepted the initial method review and the affected-item re-review.
- A3 implementation review — `gpt-5.6-terra`, high: accepted the affected-item re-review and independently reran five targeted tests: `5 passed, 12 deselected`.
- A4 synthesis — coordinator: core method and routing were accepted consistently; alternating-axis fixture, analytic-gradient, nonzero-\(t\), and route-evidence repairs are closed. CPU engineering delivery is accepted; real method effect is unverified.
- A5 final short audit — `gpt-5.6-sol`, high: accepted the frozen delivery without substantive repair after checking the frozen SHA/base, clean old worktree, formula and explicit override, receiver isolation, gradient/route/budget checks, and `16 passed, 1 deselected`. It did not rerun tests. Evidence remains CPU engineering only.

This is engineering-only CPU evidence. It does not establish detection, media persistence, SPEED5_4 improvement, GPU feasibility, or a real roster/shared-OFF comparison. When aggregate \(t\) values are near zero, the normalized new direction can nearly equal the old direction. A/B effects can mask each other and tanh saturation remains. This 11-by-2 state-MSE target is not the historical comparison of a per-block squared-hinge target and a two-message tanh competition target; historical ranking changes do not imply existence evidence, and its terminal RMS is not the current native \(R^*\).
