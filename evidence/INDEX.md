# Local experiment records

Historical fixed construction. Drive run01 root records plus separately labelled existing local independent review archived. 6 videos executed; reviewed dynamic q coverage 11/12/11/15 below 24/30. The review reports source f54917a; root result alone does not establish that SHA. No media or tensors copied; no general observer failure claim.

Raw records are local under `evidence/raw/`; see [manifest.json](manifest.json) for origins, byte sizes and SHA256. A fresh Git clone contains the index, not the downloaded raw package. Source references, failures and original statuses are retained.

| Run | Files | Provenance |
|---|---:|---|
| local_temporal_warp_run01 | 16 | drive_raw_download, existing_local_independent_review |
