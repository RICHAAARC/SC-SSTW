# Local experiment records

Historical diagnostic branch. run08 and split02 originals downloaded. split02 completed 12 Transformer / 3 VAE / 5 backward calls, returncode 0, on NVIDIA A100-SXM4-40GB (runtime.json), not evidence of successful L4 execution. It does not perform candidate writing or acceptance; only the split VJP diagnostic completed and science_denominator is 0. Runtime source SHA is not established by these root files; notebook pin 891988b is an entrypoint anchor, not retroactive run provenance.

Raw records are local under `evidence/raw/`; see [manifest.json](manifest.json) for origins, byte sizes and SHA256. A fresh Git clone contains the index, not the downloaded raw package. Source references, failures and original statuses are retained.

| Run | Files | Provenance |
|---|---:|---|
| public_statistic_after47_split02 | 8 | drive_raw_download |
| public_statistic_run08 | 9 | drive_raw_download |
