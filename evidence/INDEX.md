# Local experiment records

Active multistep branch. Only the supplied execution summary is locally archived; this is NOT a raw Drive result package. LAST/EARLY_LAST 8/8; EARLY_ONLY 6/8 or 7/8 depending on reader. This configuration did not improve the control/quality tradeoff; the branch remains active.

Raw records are local under `evidence/raw/`; see [manifest.json](manifest.json) for origins, byte sizes and SHA256. A fresh Git clone contains the index, not the downloaded raw package. Source references, failures and original statuses are retained.

| Run | Files | Provenance |
|---|---:|---|
| flow_tube_multistep_20260920T075731119017Z | 1 | user_pasted_summary |
