# after47 fixed-point diagnostic

Reuse run08 config and OFF1/terminal_float_rgb.npy. Rebuild normal steps 0 through 47 once; take the update cap RMS before step47, exactly as the original runner. From the unchanged after47 latent and full scheduler history execute gradient baseline, two no-grad baselines, fixed update and its opposite. No line search, acceptance, parameter scan or new MP4. Every rollout gets a separate history clone; original state digests are recorded before/after. No-grad comparisons distinguish repeated evaluation noise from grad/no-grad path differences without claiming a cause from two repeats.

The update is the original min(eta, cap/gradient RMS) rule. Actual FP32 subtraction and opposite-rounding error, g dot actual delta, cosine to negative gradient, and BF16 input changes are recorded. The symmetric directional difference uses epsilon=1 along that single frozen actual delta; it is not an infinitesimal derivative or a scan. A float64 loss reduction is diagnostic only; q still comes from the original readout. Output includes the reconstructed latent, gradient, actual deltas, target and full scheduler state; it does not include a model or new video.

Budget: 48 prefix steps x2 + 5 forecasts x2 remaining steps x2 =116 transformer forwards; 5 decodes, 1 backward. Additional replay maxima120 blocks /13 chunks, no MP4,1800 seconds from launcher start, zero retry. No GPU model-name gate or artificial memory quota. CUDA/BF16 remains the computation requirement. Tiny CPU tests are engineering validation; the real pretrained GPU diagnostic has not been executed.
