# Phase 2 checkpoint execution

The notebook selects `configs/public_luma_phase2_checkpoint_proposal.json`. Runs 06 and 07 reached real VAE forward and backward respectively but failed with CUDA OOM. Run08 adds boundary-only CPU storage; whole-forecast CPU activation transfer remains disabled. Run08 has CPU validation only, pending user GPU execution.

Transformer uses its official enable_gradient_checkpointing entry with a nonreentrant checkpoint function. Only differentiable forecasts enter it; normal generation remains unchanged. Replay stops once the tensors needed for backward have been reconstructed. Model weights, mixed BF16/FP32 rules, and solver state semantics are unchanged.

The FP32 AutoencoderKLWan reports unsupported generic gradient checkpointing. Its adapter instead reuses the engineering construction in archive/SC-SSTW-Feasibility/src/sstw/flow_guidance_embedder.py::_checkpoint_wan_decoder_frames. No archive code is imported. A scoped decoder.forward wrapper flattens incoming causal cache tensors, reconstructs a fresh local cache/cursor for every forward/replay, returns all outgoing cache tensors explicitly, and checks cache layout/consumption across replay. None/Rep markers and first_chunk behavior are preserved. The ordinary forward reconstructs native cache slots; backward replay never mutates the VAE global cache. Native vae.decode continues to own post_quant_conv, temporal chunk scheduling, unpatchify, clamp and clear_cache. The wrapper is restored even on exceptions. Expected chunk count is the actual latent temporal length, not a fixed13. The present proposal excludes spatial tiling and multivideo batches.

Cache tensor connections are never detached. A gradient from later output frames therefore reaches earlier latent chunks through the same causal history. Each checkpoint retains its explicit boundary inputs; internal activations are recomputed during backward. Historical checkpoint inputs are saved on CPU; the currently active native cache remains on GPU. This does not establish a measured full-model memory peak.

The existing top-level budget remains124 Transformer forwards/7 VAE decode calls/2 backwards/3 MP4/147 frames. Additional budget is a separate ledger: at most180 Transformer block replays (6 differentiable Transformer calls×30 blocks for the intended architecture) and26 decoder frame replays (2 differentiable decodes×13latent chunks). Actual loaded architecture/config is recorded and budget exhaustion is fatal; no model-version gate substitutes for actual accounting. CPU tiny3latent/one-block tests require6 extra blocks and6 extra decoder chunks. These counts use distinct units and must not be added to top-level VAE or Transformer counters as if they were the same operation. Completed replay counts include successful early termination after all needed tensors are reconstructed, with no GPU synchronize.

Internal block/chunk counters are memory-only. Existing compact top-level progress attaches their latest summary without extra per-block Drive writes; a hard kill between stages can leave that snapshot behind actual replay work. Worker cleanup writes final recomputation.json with counts, bounds and boundary-storage diagnostics. Each boundary reports logical tensor bytes and deduplicated backing-storage bytes. Per-decode phase union tracks storage identities, not live peak allocation (in particular replay allocator reuse affects that interpretation). The ledger retains only numeric records/identities, no tensors or storage objects. Independent saved-tensor storage probes are needed for memory conclusions.

CPU validation uses random real tiny AutoencoderKLWan and mixed-precision WanTransformer3DModel with real UniPC. Tests compare native vs checkpointed13latent→49RGB frames and latent gradients, cross-chunk cache derivatives, restoration, bounded replay failure, and full OFF1/OFF2/Y_MINUS flows with unchanged loss/quality acceptance. Old successful engineering is evidence for this adaptation, not evidence that the full remaining-solver scientific result or a pretrained GPU execution succeeds.

## run08 boundary storage repair

VAE checkpoint explicit input storages are copied to CPU, deduplicated within each checkpoint call. Inner activations remain under non-reentrant checkpoint control. Packed values preserve dtype, shape, stride and storage offset; restored alias inputs share a weakly cached device view. No global strong tensor pool is used. Forward native cache stays on GPU; packed CPU values follow autograd lifetime. This is not the previous whole-forecast activation offload.

Default checkpoint early stopping is restored. Successful early-stopped replays count as completed, without requiring the decoder to finish unnecessary trailing work for accounting. Recompute boundary rows may be absent because the wrapper does not return normally; replay counters still record success. Original workload and scientific settings remain unchanged. The text encoder is discarded directly after prompt encoding, avoiding an unnecessary discarded CPU copy.

CPU output/gradient, 13-chunk temporal dependency, packed-input-only and release tests pass. PyTorch 2.11.0 source confirms input saver creation precedes the inner checkpoint hook. Actual A100 and host RAM peaks remain unverified: historical boundary storage adds roughly 16.58 GiB CPU residency at full size, and transfer time is not measured locally.
