# Fixed one-sided signed-margin writer candidate

Method hypothesis: the MSE writer pulls every coefficient toward a point target, including already-correct strong coefficients. Replace only this objective with L=.5 sum(relu(.5-b*d)^2), where d is the existing adjacent-time difference and b is the same keyed payload sign. Active gradient is -b*relu(.5-b*d); inactive coefficients have zero gradient. Preserve the carrier,mask,keys,16bitpayload,92hardvotes,eta.1,window30..49,CFG5 and native50step solver. No changed receiver threshold, hybrid soft selection, ECC, pair selection or strength scan. This is an unvalidated hypothesis, not a repair proven by saved-data analysis.

runtime.grow_temporal_difference uses explicit optional loss_fn; None defaults to original method.loss and old observations, without global monkeypatching. Margin runner passes grow_signed_margin.loss. Readout stays unchanged. Local observations include both old candidate MSE and actual writer objective losses; terminal reports both. Local/native-shadow control energy and final-step effect are preserved. The one-sided local update also yields loss ratio.81 in this orthonormal projected domain at eta.1; no claim about later model dynamics follows.

The new full runner generates all4case xOFF/A/B=12 then attempts all48media layers with no terminal gate. Each generation case and each media case runs in a separate child process; generation children exit before VAE children begin. A missing generation artifact retains missing media slots. New source results/hashes/config are validated by the same media adapter with explicit expected run/commit/manifest; its defaults still target the old media source. Actual effective manifest and expected provenance are saved. No historical OFF reuse.

Budget by stage: generation1200Transformer/600live/160localgradient/160shadow; media12decode/36encode/12save/12read, zeroTransformer. Combined denominator is12videos, not24; layers48, marked8, bits128 per layer. Preserve all failures, original and intermediate tensors, OFF coincidence observations, actual controls, per-pair hard/continuous evidence, media quality diagnostics and stage/total counts. No metric threshold is introduced. Human review of prompt content/artifacts/flicker/motion/coherence is necessary; paired PSNR is not visual quality PASS. FPR/existence calibration, ablations and generalization remain outside this run.

The new fixed Run-all notebook uses GROWTemporalMargin/grow_temporal_margin_<UTC> with generation/media subdirectories, source.zip and streamed launcher/case logs. It retains the existing working dependency versions without GPU-model gate. Source SHA is pinned after publication. CPU/Fake verification is engineering-only; no real model/GPU/media run has occurred during implementation.
