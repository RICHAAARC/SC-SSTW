# Existing LAST detection run: CPU synchronization diagnostics

This entry reads only JSON from fixed run `flow_tube_detection_20260920T113113319009Z`, original source `f561fd58f01c8cc44a05885c0eb9ff42017465c7`. It preserves every original detection statistic, acceptance decision, A/B attribution, threshold and sequence decision. No generation, VAE encoding, tensor/media loading, receiver rerun, candidate search, reranking or recalibration is performed. It adds zero independent scientific samples.

The denominator is two evaluation cases × OFF/LAST_A/LAST_B × seven views = **6 sources / 42 views / 462 reference-window slots**. Four sources are marked and two are OFF. Nine calibration sources are excluded from this view denominator; their original frozen threshold JSON is read and SHA-checked against both evaluation records. Six original sequence aggregates are copied separately and remain source-level correlated summaries, not additional samples.

Inputs are the two case result/config JSONs, frozen threshold, and 42 per-view `blind.json` files. Each full receiver file is verified against the SHA in its original case result before diagnostics. The large blind files are read one at a time; they need not be copied or committed. A missing full receiver retains a row and, when available, reports the selected geometry from the original case result; it does not fabricate full candidate/class evidence. New output and original input directories must not overlap. Output includes a compact result and input SHA manifest, never changes original inputs.

For the persisted five rankings, copy the selected path and top ties without resorting. Report the 4284 existing paths (204 global settings with no change or one local ±1 RGB offset step), stored observation classes, selected-class path members and top-tie classes. Show the complete eleven-window origin/scale/offset/group path and check it against unchanged allocation. These are bounded paths, not arbitrary edit dynamic programming. For each unique top-tie class, append nominal support coverage after ranking; never choose a winner using the known attack map. Existing crop reference reports are preserved. Observation equivalence alone is not a proof of exact source-time compatibility.

Each group is assigned its nominal four source RGB indices. Report the actual received-to-source list, overlap, missing/extra source indices and center error. Keep all eleven windows, with valid, equal nominal support, partial/different support and unobserved/incomplete status separate. Count nominally available windows separately. A missing window never earns equality credit. Repeated observed groups or source indices are explicit.

For `delete90`, the crossing window can cover 15/16 nominal source frames while ten other windows have equal nominal supports. For `speed125`, nearest-frame resampling necessarily omits source frames: even a reasonable path can cover 13/16 per valid window, and the unchanged no-reuse/full-four-group allocation can leave only three valid windows. Zero exact-support windows under speed must **not** be presented as a 0/11 synchronization success rate. Support equality, geometric coverage, candidate compatibility and exact timing are distinct. These groups are not VAE receptive fields; different phase encodes also have different prior context. Exact synchronization, VAE equivalence and exact edit-frame localization remain null/unproven, especially for delete/speed.

For the saved selected class, recompute the existing observer from its q/valid entries and the original public key: transition by reference index n; invalid or zero-q windows propagate prediction without a measurement update and receive innovation cost 1. Compare stored predictions, updated states, innovation and matched/state/no-update scores, plus each ranking's stored best observer and event-cost score. This is a consistency check of the old algorithm, not an observer modification or a new test set. No truth enters observer computation; known frame maps are used only for post-read support reporting.

The notebook has an independent two-line Drive mount and fixed input run, fetches an immutable diagnostic source SHA, and Run all launches CPU-only JSON processing into a new `FlowTubeSyncDiagnostics` directory. NumPy is its only numerical dependency. Engineering tests use synthetic geometry, persisted-schema fixtures and missing/hash cases. They establish implementation behavior, not new watermark robustness or FPR.
