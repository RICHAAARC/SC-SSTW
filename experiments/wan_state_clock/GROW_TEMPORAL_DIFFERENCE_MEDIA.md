# Saved GROW temporal-difference media validation

Fixed input is grow_temporal_difference_20260918T142825573989Z generated by a004134f650267485c0b80abdfbb4062fb7c7128. All four cases xOFF/A/B=12 are retained, without regeneration or Transformer loading. Verify source root/case recorded hashes, terminal file SHA256, fixed config, latent geometry and complete recomputed terminal readout before VAE use. Missing source model revision is recorded as an exact checkpoint identity limitation, not a hardware gate. Source directory is immutable; a new output directory is required.

Each verified terminal has exactly one FP32 frozen Wan VAE decode using existing normalized-latent mean/std helpers. Float decoded RGB, its fixed rint RGB8 quantization and CRF18/yuv420p MP4 roundtrip each undergo one deterministic posterior-mode VAE reencode. Read all three and original terminal with unchanged temporal-difference method:46raw slices,23pairs,4repeats,92votes/bit,16bits. Fixed48 layers include all failures. Budget12decode/36encode/12MP4save/12MP4read; no generation, gradients, scans, or attacks.

Persist copied terminal and three reencoded latent tensors, float decoded RGB, original uint8 and MP4-readback uint8 pixels, MP4 files and hashes. Full RGB evidence is about6.4GB decimal across12clips (4.27GB float plus2.14GB uint8), plus tensors/video/container overhead. No space/model gate or automatic deletion is added. Final layers enable causal localization of loss to VAE reconstruction, quantization, or codec chain without extra generated samples. Saved MP4 raster is reconstructible by /255. Failures in saving prevent reading a residual MP4; float/RGB8 branches remain measured.

Quality metrics compare each arm to same-case OFF at the same RGB layer, with PSNR/MSE and temporal residual metrics as diagnostics only; no quality tolerance or scientific PASS. Human inspection must examine prompt adherence, objects/details, artifacts, flicker, motion and temporal coherence in OFF/A/B. Decode/MP4 recovery, human quality, attacks, FPR and generalization are separate claims. OFF exact-message coincidences are descriptive, not a calibrated false-positive test.

The fixed Run-all notebook mounts Drive first, checks out published immutable media source, and reads the exact input directory. Outputs use GROWTemporalDifferenceMedia/grow_temporal_difference_media_<UTC>, with source.zip and launcher/child logs. No automatic further experiment or multi-step/last-step ablation is included. CPU/Fake tests cover source integrity, shared decode/four layers, residual MP4 failure isolation, fixed denominators, source nonoverwrite and zero Transformer budget. Real GPU/media execution is left to the user.
