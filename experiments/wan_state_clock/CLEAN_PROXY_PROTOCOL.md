# Frozen T46 clean-proxy objective comparison

Frozen before implementation or real execution. Source inputs: flow_tube_response_selection_20260921T013844126172Z, generation source9fdfac97126fa681c42423089eb342ce85b676a7. Two original dev cases dev_p0_s0/dev_p1_s0 and their original content/seed, carrier/state×polarity×sync, T46, A/B, native50 UniPC and original reader are fixed.

Each case has exactly OFF, HINGE_A/B and TANH_A/B:10 formal outputs total. All start from SHA-verified saved OFF_nodes z46/v46 and the complete saved UniPC46 snapshot. Snapshot is not reconstructed from a timestep: retain all model_outputs, last_sample, lower-order/this-order state, cursors, sigma/timestep schedule and configuration. Restore saved prompt/negative embeddings and original codebook/config; no prefix sampling or text encoding. Each path gets a fresh deepcopy, with original history/input fingerprints checked.

The original recorded model revision is None. Resolve and record the newly loaded model revision where possible; do not claim original weight-byte identity. Formal OFF is a new zero-control native46 step and free47–49 tail from the original snapshot. Compare its terminal against historical OFF with the existing maxabs tolerance (atol2e−5, rtol2e−4); historical nonmatch is reported separately and does not replace or invalidate the same-batch new OFF/new controlled-arm comparison. Historical OFF is never the sole formal quality/media baseline.

For each of four controlled arms, compute clean46=z46−sigma46*v46. On a detached CPU float64 leaf use old hinge L=.5mean relu(1−c_m p)^2 or the already frozen temperature1 Ltanh=−mean tanh(p)(c_m−c_other). These are current-clean proxy gradients, not terminal-gradient transport. Fix d=−gradient, normalize by RMS on original support1:45. Each arm independently performs one native unit probe from the original history with velocity v46−q/sigma46. Target R is the same-message historical LOCAL actual native D support RMS from the verified generation record, not the earlier offline terminal residual RMS. Set epsilon=R/RMS(Dunit) once, no scan/clipping/backtracking, then independently replay a fresh original history with u=epsilon*q and v46−u/sigma46. Free steps47–49 under no_grad. Zero/nonfinite/unmatchable gradients retain failed slots with no fallback.

Use the newly generated OFF native47 state as the same-history response reference Dunit/actualD. It is determined by the original z46/v46/history, not new tail weights. Save actualD/u/delta-velocity/epsilon/target/matching errors, input/history fingerprints, gradient critical/noncritical and saturation shares, current-clean and actual terminal nominal statistics. Report early control transmission as observed statistics, not guaranteed by offline objective alignment.

Minimum fixed new calls per case: OFF6TF/4native; four controlled formal tails24TF/16native; four unit-native probes; four CPU clean-leaf backwards. Whole run60 Transformer forwards /40 native /8 unit probes /8 CPU leaf backward. No generation-model backward or VAE backward, no fresh prefix or prompt encoder. Counters separate attempted/completed calls and per-arm timing; failures retain all denominators.

Every formal output, including new OFF, is decoded and saved to a new MP4. All10 videos have three actual media layers: decoded floatRGB VAE roundtrip; np.rint RGB8/255 without codec VAE roundtrip; saved MP4 true readback VAE roundtrip. Each layer has original four phase origins with181/177/177/177 frames, four independent VAE encodes and original five-mode receiver. Native terminal is a separate full-support nominal statistic, never fabricated into four-phase observations. Whole media budget10 decode /10 MP4 save /120 VAE encode, fixed30 layer-readouts/120 phase slots. Layers/arms fail independently and missing results stay explicit. No terminal metric or receiver score gates media production.

Report absolute target/wrong nominal scores and gap at terminal, and target/wrong best-message scores plus gap/unique-correct at each original receiver mode/layer. Primary blind receiver remains local_state, with correct ranking distinguished from positive gain and existence detection. Old detection tau is not a success threshold for this exploratory comparison. Same-case/message hinge-vs-tanh and each-vs-newOFF comparisons retain fixed4pairs. Float/RGB8/MP4 quality differences reference only this batch's new OFF at the corresponding raster layer. These differences are not perceptual approval; manual content/artifact/motion checks remain outstanding.

Old entrypoints and successful baselines remain unchanged. Source/protocol/input hashes are persisted. Notebook begins with independent two-line Drive mount, pins a published full source SHA and exact original run path, and Run all executes this fixed experiment. User runs the models; implementation performs only CPU/fake tests. No automatic GPU execution, parameter choice by results, carrier replacement, extra timestep or new codebook.
