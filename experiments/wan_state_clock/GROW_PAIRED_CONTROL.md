# Prepared MSE paired-control experiment: multi-step versus last-step

This turn authorizes implementation and synthetic engineering checks only. No real generation, GPU experiment, new scoring of saved scientific observations, Drive result analysis, parameter fitting, or scientific conclusion is performed. User notebooks are prepared for later explicit execution. The trajectory mechanism remains primary; no inversion, tube-carrier or media-aware writer is introduced here.

Keep the original adjacent-time-difference carrier,23pairs,4frequency repeats,16bits/92votes,channel0 spatial mask2..9,key,payloads,eta.1,amplitude.5,CFG5 and50step native UniPC. Writer is original half-squared MSE, explicitly not the one-sided margin candidate. MULTI applies control at30..49. LAST uses the same initial noise, prompt, negative conditioning and original schedule, no prior control, and controls49 only. OFF is generated once per case and reused as media quality reference. Four cases x five arms OFF/MULTI_A/MULTI_B/LAST_A/LAST_B produce20videos and80media layers, without terminal-based filtering.

## Actual control cost and deterministic matching

For each live controlled step, clone the same native scheduler history and state. Define delta_i = native_step(v_control,z_i,H_i) - native_step(v_off,z_i,H_i). The measured discrete local response cost is E=sum_i mean(delta_i^2). This is not terminal energy, accumulated displacement energy, a physical time integral, or sum of u²; step spacing is not integrated. Each message/case's MULTI measuredE is the budget for its paired LAST arm. Matching uses no decoder result, quality score or truth-based choice.

At LAST49, obtain an OFF shadow and one eta.1 base-control native probe from the identical state/history. E_unit=mean((probe-off)^2). Native fixed-history current-output dependence is linear in the model velocity under the required unthresholded flow/predict_x0 solver contract. Set scale=sqrt(E_multi/E_unit), then perform exactly one scaled live update. No scan, retry, cap, quality repair, or learned correction. E_unit0 with positive target is unmatchable and retained as a failure; bothzero use scale0. Numerical achievedE/absolute/relative error are reported without adjusting to pass. Fixed finite checks prevent invalid tensors, not unfavorable results.

The target depends on its paired MULTI realization and message, so this is a paired realized-budget ablation, not a universal calibrated strength. Equal local response cost does not imply equal peak control, total u², delta-velocity², final difference or visual distortion. Report all these diagnostics. A large LAST control or worse quality is retained. Final sigma-to-zero semantics can directly expose the last controlled predicted-clean; MULTI success alone is not evidence that earlier trajectory control added value.

Plan per case:500Transformer,250live steps,42local gradients,42same-history OFF shadow calls plus2base-control unit probes. Across4:2000/1000/168/168/8 respectively. Probe categories are disjoint. Media adds20decode/60encode/20MP4save/read with no Transformer. Every generation case and media case is a separate child; no Transformer survives into VAE memory. Missing generation/budget retains five generation slots and all media slots. No terminal gate selects media.

Every media observation is scored by both unchanged hard bit recovery and fixed raw-soft recovery, threshold0 with zero/ties erased. Separately report continuous uniform correlations against public A/B payloads, top/tie/gap, then join truth for evaluation. A two-candidate attribution is not complete16bit recovery or watermarked existence detection. MULTI/LAST each retain8marked/128bits per layer; do not pool them to conceal timing effects. OFF is a reference, not calibratedFPR. No choice of whichever decoder works is permitted. Historical raw-soft negative evidence remains relevant; no real re-scoring has occurred this turn.

Preparation also includes a CPU-only notebook fixed to the old MSE media result grow_temporal_difference_media_20260918T171544536253Z. It reads existing same-carrier aggregates only and saves both rules, all48rows, candidate scores and input/scorer hashes. This is a separate readout comparison, not a third experiment branch, and was not run on real data this turn.

The future GPU Run-all notebook uses GROWPairedControl/grow_paired_control_<UTC>. It reports8paired energy rows,2timing x2reader x4layer results and separate stage counts. Generation/media tensors, configs, source/model revision metadata, hashes and logs are retained; approx10.7GB RGB intermediates plus other artifacts are expected. Paired PSNR remains descriptive; inspect prompt content, artifacts, flicker and temporal coherence manually. No hardware-model/quality gate or automatic subsequent experiment is added. Current tests use synthetic tensors and fake models with native scheduler code; they do not establish real feasibility or method benefit.

Hard candidate correlation uses normalized sign-vote scores; soft candidate correlation uses raw coefficient means. Their units and scales differ. Do not directly compare their gaps as increased confidence or use cross-reader gap magnitude to choose a receiver.
