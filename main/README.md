# Core Method Packages

sc_sstw contains carrier-independent interfaces for affine-invariant public
acquisition, public-only calibration and state-constrained synchronization. It
must not import runtime, experiment records, notebooks or governance code.

Constructed channels, synthetic state generation and scoring aids belong to
experiments/feasibility/synthetic, not this core package.
